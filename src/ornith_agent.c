/* ornith_agent.c — integrated coding agent.
 *
 * STATUS: stub. Ornith-1.0 is post-trained for agentic coding, so a built-in
 * agent (read/write files, run commands, apply diffs) is a first-class goal,
 * not an afterthought. It will drive the local server's tool-calling loop.
 * Parked until the engine and server land (ROADMAP M5).
 */
int ornith_agent_main(int argc, char **argv) {
    (void)argc; (void)argv;
    return 64; /* not implemented */
}
